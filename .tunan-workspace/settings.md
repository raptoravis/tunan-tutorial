---
current_sprint: S01
---

# tunan workspace settings

To switch sprint: change `current_sprint` to the next `S<MM>`, then create both `sprints/S<MM>/reqs/` and `raws/S<MM>/` (each with a `.gitkeep`).
