{
  "name": "tunan-tutorial-voting",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "concurrently -n server,web -c green,cyan \"pnpm -F server dev\" \"pnpm -F web dev\"",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "packageManager": "pnpm@11.1.1"
}
