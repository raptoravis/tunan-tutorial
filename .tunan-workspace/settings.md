---
current_sprint: SPT-001
---

# tunan workspace settings

To switch sprint: change `current_sprint` to the next `SPT-NNN`, then create both `sprints/SPT-NNN/reqs/` and `raw-reqs/SPT-NNN/` (each with a `.gitkeep`).
