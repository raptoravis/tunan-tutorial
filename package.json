{
  "name": "tunan-tutorial",
  "private": true,
  "engines": {
    "node": ">=22.5"
  },
  "scripts": {
    "dev:server": "pnpm -F server dev",
    "dev:web": "pnpm -F web dev",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r test",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
